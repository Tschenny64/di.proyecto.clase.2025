<mah:MetroWindow x:Class="UD4_Ejemplo1.Frontend.Dialogos.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:local="clr-namespace:UD4_Ejemplo1.Frontend.Dialogos"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>

        <!-- Columna izquierda -->
        <Grid Grid.Column="0">
            <!-- Círculo decorativo -->
            <Ellipse Width="550" Height="550"
               HorizontalAlignment="Left"
               VerticalAlignment="Center"
               Margin="-150,0,0,0">
                <Ellipse.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="Black" Offset="0"/>
                        <GradientStop Color="DarkGoldenrod" Offset="1"/>
                    </LinearGradientBrush>
                </Ellipse.Fill>
            </Ellipse>
            <!-- Textos encima del círculo -->
            <TextBlock Text="IES HENRI MATISSE"  
                 Foreground="White" 
                 Margin="50,0,0,0" 
                 VerticalAlignment="Center" 
                 FontSize="25" 
                 FontWeight="Bold"/>
            <TextBlock Text="Aplicación de Inventario"  
                 Foreground="White" 
                 Margin="50,50,0,0" 
                 VerticalAlignment="Center" 
                 FontSize="12" />
        </Grid>

        <!-- Columna derecha: formulario login -->
        <Grid Grid.Column="1" Margin="0,50,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- IMAGEN -->
            <Image Source="/Resources/Iconos/user.png" Grid.Row="0"
             HorizontalAlignment="Center" 
             Height="80"/>

            <!-- TEXTO ABAJO IMAGEN -->
            <TextBlock Grid.Row="1"
                 Text="Introduce los datos"
                 Foreground="Gray"
                 Margin="10"
                 HorizontalAlignment="Center"/>

            <!-- USERNAME -->
            <TextBox x:Name="txtUsuario" Grid.Row="3" HorizontalAlignment="Center" Width="300" Margin="0,30,0,0"
   Style="{StaticResource MaterialDesignOutlinedTextBox}"
   materialDesign:TextFieldAssist.HasClearButton="True"
   materialDesign:TextFieldAssist.TextFieldCornerRadius="25">
                <materialDesign:HintAssist.Hint>
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 0">
                        <materialDesign:PackIcon Kind="Email"/>
                        <TextBlock> Cuenta de Correo</TextBlock>
                    </StackPanel>
                </materialDesign:HintAssist.Hint>
            </TextBox>

            <!-- PASSWORD -->
            <PasswordBox x:Name="txtContrasena" Grid.Row="4" HorizontalAlignment="Center" Width="300" Margin="0,30,0,0"
   Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
   materialDesign:TextFieldAssist.HasClearButton="True"
   materialDesign:TextFieldAssist.TextFieldCornerRadius="25">
                <materialDesign:HintAssist.Hint>
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 0">
                        <materialDesign:PackIcon Kind="Password"/>
                        <TextBlock> Clave de Acesso</TextBlock>
                    </StackPanel>
                </materialDesign:HintAssist.Hint>
            </PasswordBox>

            <!-- LOGIN -->
            <Button Content="Login"
        Grid.Row="5"
        Margin="80,20,80,0"
        Foreground="White"
        x:Name="btn_login"
        Click="btn_login_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="25" BorderThickness="0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="Gold" Offset="0"/>
                                    <GradientStop Color="Green" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <!-- FORGOT -->
            <TextBlock Text="Nuevo Usuario?" Grid.Row="6" 
                 HorizontalAlignment="Left" Foreground="Gray" Margin="100,10,20,20"/>
            <Button Content="Registrate" Grid.Row="6" Margin="100,0,0,20"
              
              Background="Transparent" 
              BorderBrush="Transparent" 
              Foreground="Green" 
              HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</mah:MetroWindow> 
